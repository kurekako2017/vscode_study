/** @type {import('next').NextConfig} */
// Next.js 项目级配置。
// 这里先保持最小配置，方便学习时把重点放在 App Router、组件和 TypeScript 数据流上。
const nextConfig = {
  // reactStrictMode 会在开发环境帮助发现副作用和不稳定渲染问题。
  reactStrictMode: true
}

export default nextConfig
