# 全局前后端处理流程图

本文用于学习 `JtProject-Next` 的主框架调用链。当前推荐优先看 **Next.js 前端 + Spring Boot API 后端** 这条流程；`src/main/webapp/views/*.jsp` 是旧版 Spring MVC 页面，对照学习时再看。

## 1. 前端处理流程图

![前端全局处理流程图](assets/global-frontend-flow.svg)

<details>
<summary>Mermaid source</summary>

```mermaid
flowchart TD
    A["浏览器访问<br/>http://localhost:3000"] --> B["Next.js App Router<br/>frontend/app/layout.tsx"]
    B --> C["首页 Client Component<br/>frontend/app/page.tsx"]
    C --> D["初始化 React state<br/>products/cart/session/message/loading"]
    D --> E["useEffect 首次挂载<br/>loadInitialData()"]
    E --> F["Promise.all 并行请求<br/>/api/session + /api/products"]
    F --> G["前端 API 调用层<br/>frontend/lib/api.ts<br/>函数: api<T>()"]
    G --> H["浏览器 fetch 层<br/>fetch(API_BASE + path)<br/>credentials: include"]
    H --> I["后端 Controller 层<br/>ApiController.java<br/>@RestController /api"]
    I --> I2["后端业务与数据层<br/>Service -> DAO -> Hibernate -> DB"]
    I2 --> J["Controller 返回 ApiResult<T><br/>{ success, message, data }"]
    J --> K["api<T>() 解析 JSON<br/>失败时 throw Error"]
    K --> L["page.tsx setState<br/>setSession/setProducts/setCart"]
    L --> M["React 重新渲染 JSX<br/>显示商品、登录状态、购物车"]

    N["用户操作<br/>登录/加购物车/删除/刷新"] --> O["事件处理函数<br/>submitUserLogin/addToCart/removeFromCart"]
    O --> P["runAction()<br/>设置 loading + try/catch"]
    P --> G
```

</details>

### 前端图分层对照

| 图中节点 | 实际文件/类 | 属于哪一层 | 作用 |
| --- | --- | --- | --- |
| 首页 Client Component | `frontend/app/page.tsx` | 前端页面层 / React UI 层 | 保存页面 state，处理按钮和表单事件 |
| 前端 API 调用层 | `frontend/lib/api.ts` | 前端数据访问层 / API Client 层 | 统一封装 `fetch`、cookie、JSON 解析和错误处理 |
| 浏览器 fetch 层 | 浏览器内置 `fetch()` | HTTP 通信层 | 真正向 `http://localhost:8086/api/*` 发请求 |
| 后端 Controller 层 | `src/main/java/.../controller/ApiController.java` | Spring MVC Controller 层 | 接收 `/api/*` 请求，校验参数/session，调用 Service |
| 后端业务与数据层 | `services/*`、`dao/*`、`models/*` | Service / DAO / Model 层 | 执行业务逻辑、数据库访问和实体映射 |

## 2. 后端处理流程图

![后端全局处理流程图](assets/global-backend-flow.svg)

<details>
<summary>Mermaid source</summary>

```mermaid
flowchart TD
    A["前端 fetch 请求<br/>/api/session /api/products /api/cart ..."] --> B["Spring Boot 内嵌 Tomcat<br/>JtSpringProjectApplication"]
    B --> C["Spring MVC DispatcherServlet<br/>按 URL + HTTP Method 分发"]
    C --> D["CORS + Cookie/Session<br/>允许 localhost:3000 携带凭证"]
    D --> E["ApiController<br/>@RestController @RequestMapping('/api')"]
    E --> F{"接口类型"}

    F -->|公开查询| G["products/categories<br/>调用 ProductService/CategoryService"]
    F -->|用户接口| H["login/cart/logout<br/>校验 username、role、session"]
    F -->|管理员接口| I["admin/*<br/>校验 ADMIN_LOGGED_IN"]

    G --> J["Service 层<br/>业务编排"]
    H --> J
    I --> J
    J --> K["DAO 层<br/>UserDao/ProductDao/CartDao/..."]
    K --> L["Hibernate SessionFactory<br/>HibernateSessionFactoryConfig"]
    L --> M["数据库<br/>CUSTOMER / PRODUCT / CATEGORY / CART / CART_PRODUCT"]
    M --> L
    L --> K
    K --> J
    J --> N["Controller 组装返回数据<br/>Map/List DTO"]
    N --> O["统一 JSON 结构<br/>{ success, message, data }"]
    O --> P["ResponseEntity + HTTP Status<br/>200/201/400/401/404/409"]
    P --> Q["返回给前端 api<T>()"]

    E -.旧版页面对照.-> R["UserController/AdminController<br/>返回 JSP 视图名"]
    R -.-> S["InternalResourceViewResolver<br/>/views/*.jsp"]
```

</details>

## 3. 学习顺序

1. 先看 `frontend/app/page.tsx`：理解页面状态、事件处理、`useEffect` 初始化。
2. 再看 `frontend/lib/api.ts`：理解前端如何统一调用后端。
3. 再看 `src/main/java/.../controller/ApiController.java`：理解每个 `/api/*` 请求怎么进入后端。
4. 最后顺着 `Service -> DAO -> models -> database` 看数据怎么被读取和修改。
