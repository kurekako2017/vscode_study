# 简单测试用例表

## 1. 说明

本用例表用于对 `tool_agent_demo` 做最小验证。

重点不是覆盖所有复杂场景，而是确认这几个核心点：

- 工具调用闭环是否成立
- 文件访问边界是否受控
- 回答是否基于工具结果
- 常见异常是否能被基础处理

## 2. 测试用例

| 用例ID | 分类 | 测试内容 | 前提条件 | 预期结果 |
|---|---|---|---|---|
| `TAD-01` | 启动 | 未设置 `OPENAI_API_KEY` 启动程序 | 未设置环境变量 | 程序报错并退出 |
| `TAD-02` | 启动 | `--workdir` 指向不存在目录 | 指定不存在路径 | 程序报错并退出 |
| `TAD-03` | 目录 | 请求列出当前目录文件 | `--workdir` 为有效目录 | 模型可调用 `list_files`，并返回目录内容相关回答 |
| `TAD-04` | 文件读取 | 请求读取某个 README 文件 | `--workdir` 下存在目标文件 | 模型可调用 `read_file`，并根据文件内容回答 |
| `TAD-05` | 文本搜索 | 请求搜索关键字 | `--workdir` 下存在目标关键字 | 模型可调用 `search_text`，并返回命中文件和内容相关回答 |
| `TAD-06` | 无命中 | 搜索不存在的关键字 | 工作目录可访问 | 返回无命中或未找到的合理说明 |
| `TAD-07` | 路径边界 | 试图访问 `..` 越界路径 | `--workdir` 为受限目录 | 工具执行失败，不能访问工作目录外文件 |
| `TAD-08` | 类型校验 | 对目录执行 `read_file` | 目标路径是目录 | 返回“不是文件”类错误 |
| `TAD-09` | 类型校验 | 对文件执行 `list_files` | 目标路径是文件 | 返回“不是目录”类错误 |
| `TAD-10` | 文本格式 | 读取非 UTF-8 文件 | 工作目录中存在非 UTF-8 文件 | 返回格式错误，不崩溃 |
| `TAD-11` | 多轮工具 | 任务需要先列目录再读文件 | 工作目录中存在多个候选文件 | 模型可进行多轮工具调用并输出最终结果 |
| `TAD-12` | 回答依据 | 检查最终回答是否引用工具结果 | 存在可验证文件内容 | 回答与工具结果一致，不虚构未读取内容 |

## 3. 建议测试数据

可以准备一个最小测试目录，包含以下内容：

- `README.md`
- `notes.txt`
- 一个子目录
- 一个包含关键字的文本文件
- 一个非 UTF-8 或不可正常读取的文件

这样可以一次覆盖：

- 列目录
- 读文件
- 搜索文本
- 异常系

## 4. 执行结果记录建议

学习阶段可以按最简方式记录：

- 执行日期
- 执行人
- 用例 ID
- 结果
- 备注

如果后续要更像现场资料，可以再扩成：

- 实际输入 prompt
- 模型名
- 命中的工具
- 输出摘要

## 5. 代价与注意点

- 这是简化版用例表，不是正式企业级测试规格书。
- 实际调用模型会产生 API 成本。
- 模型对是否发起工具调用存在一定波动，因此测试时应关注“整体链路是否成立”，不要把每次表述完全一致当成唯一标准。
- 当前案例仍是学习版，只覆盖只读工具调用，不覆盖数据库、外部 API、写操作和复杂 Agent 任务编排。
