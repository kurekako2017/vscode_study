"""
文件功能概述：`code/C1/fix_nltk.py` 主要是 fixnltk，这个文件里有 0 个类、0 个函数，主要用来串起当前章节的处理步骤。

主要函数/类的处理流程：
1. 这个文件没有独立类或函数，主要靠模块级代码直接执行。
"""

print("nltk 不可用时的离线替代：无需下载 punkt 或 tagger。")
print("如果后续要使用真实 NLTK，可在联网环境中安装 nltk 后再运行。")
