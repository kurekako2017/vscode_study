# 03_AI核心知识

> 最后更新：2026-07-17（本轮补齐：技术知识点表 + 真实版本；版本取自仓库依赖文件与本机解析）  
> 项目正式名称：**Enterprise Retail Intelligence Platform（ERIP）V1.0**  
> 早期 MVP 历史名称：Retail Insight AI（本文仅此处出现一次，不作当前名称）  
> 知识以当前仓库源码、PostgreSQL Schema 与正式页面为准，不得把未交付能力写成现状。

**版本权威来源（面试时这样说）：**

| 来源 | 路径 | 含义 |
|---|---|---|
| Backend 锁定声明 | `backend/requirements.txt` | pip 安装必须满足的 pin |
| Frontend 声明范围 | `frontend/package.json` | npm 允许的 semver 范围（`^`） |
| Frontend 解析版本 | `frontend/package-lock.json` | 当前 lock 实际解析到的版本 |
| 运行时镜像 | `docker-compose.yml` / `backend/Dockerfile` / `frontend/Dockerfile` | Compose/生产镜像标签 |
| 本机 venv 实测 | `backend/.venv` + `pip freeze` | 开发机已安装版本（应与 pin 一致） |

---

## 第一章 知识服务于企业交付

ERIP 是真实企业交付的零售经营智能平台，不是练习项目。知识点必须能映射到：

```text
登录/JWT/RBAC
→ 文書管理（Upload / Import / Chunk）
→ RAG検索 / Citation
→ 显式 AI分析（low_cost）
→ 生成取締役会报告（high_quality）
→ Report / ReportVersion
→ 提交 Approval
→ manager 审批
→ Persistent Audit
→ LLM Usage Ledger
```

【TL Review】

- 是否能把知识点连到正式页面与 API。
- 是否能说明客户为什么需要该能力。
- 是否诚实区分“默认 stub 验收”与“真实付费 LLM smoke”。
- 是否能报出**真实版本**与版本来源文件，而不是只背技术名词。

【源码映射】

```text
frontend/src/App.tsx
backend/app/api/
backend/app/services/
backend/app/llm/
backend/app/security/
backend/app/repositories/postgres/
backend/alembic/versions/
```

---

## 第一章 B 技术知识点总表（必须会背的填写版）

> 本节是面试材料的**技术知识点表正文**，不是表头模板。  
> “声明版本”= requirements / package.json；“解析/安装版本”= lock 或 venv 实测。

### B.1 总表：语言 / 运行时 / 镜像

| 技术 | 角色 | 声明/镜像版本 | 解析或实测 | 来源文件 | 项目中的用途 | 面试一句话 |
|---|---|---|---|---|---|---|
| Python | Backend 语言 | **3.12**（镜像 `python:3.12-slim`） | 本机常见 3.12.x（例 3.12.3） | `backend/Dockerfile` | FastAPI 运行时 | 企业 API 用 3.12 |
| Node.js | Frontend 工具链 | **22**（镜像 `node:22-alpine`） | 本机可有 22.x（例 22.22.1） | `frontend/Dockerfile` | Vite build / vitest | 构建用 Node 22 |
| npm | 包管理 | 随 Node 22 | 例 10.9.4 | 本机 node | `npm ci` / test | lockfile v3 |
| PostgreSQL | 正式数据库 | **16**（`pgvector/pgvector:pg16`） | Compose/宿主 PG16 | `docker-compose.yml` | 业务权威状态 | 正式 Repository |
| pgvector | 向量扩展 | 随 `pgvector/pgvector:pg16` 镜像 | 镜像内扩展 | `docker-compose.yml` | chunk embedding 存储能力 | 与 PG 一体 |
| Nginx | Compose Frontend | **1.29-alpine** | 镜像标签 | `frontend/Dockerfile` | SPA + `/api` 反代 | 8080 入口 |
| Docker Compose | 编排 | project name `erip` | compose 文件 | `docker-compose.yml` | postgres/backend/frontend | 单机验收 |

### B.2 总表：Backend Python 依赖（requirements + venv）

| 包名 | requirements.txt pin | venv 安装版本 | 用途 | 关联模块 | 关键 API/能力 |
|---|---|---|---|---|---|
| fastapi | **0.136.3** | 0.136.3 | HTTP 边界、DI、OpenAPI | `backend/app/main.py`, `api/*` | `/api/v1/*`, `/health` |
| starlette | （FastAPI 依赖） | **1.3.1** | ASGI 基础 | 由 FastAPI 引入 | 中间件/路由底层 |
| uvicorn | **0.49.0**（`uvicorn[standard]`） | 0.49.0 | ASGI server | Dockerfile / start scripts | 进程入口 |
| pydantic | **2.13.4** | 2.13.4 | 请求/响应 schema | `backend/app/schemas/*` | 统一校验 |
| pydantic-settings | **2.14.1** | 2.14.1 | 配置加载 | `backend/app/config/*` | Settings |
| SQLAlchemy | （随 Alembic/运行栈安装） | **2.0.51** | ORM/Engine | `repositories/postgres/*` | PG 访问 |
| alembic | **1.16.5** | 1.16.5 | Migration | `backend/alembic/*` | head `20260717_08_ai_runtime` |
| psycopg | **3.2.9**（`psycopg[binary]`） | 3.2.9 + binary 3.2.9 | PG 驱动 | DATABASE_URL `postgresql+psycopg://` | 连接 PG |
| httpx | **0.28.1** | 0.28.1 | 出站 HTTP（Provider 等） | `providers/*`, LLM 调用 | 外部 API |
| PyJWT | **2.10.1** | 2.10.1 | JWT 编解码 | `security/jwt_*` | login / Bearer |
| passlib | **1.7.4** | 1.7.4 | 密码哈希框架 | `security/password.py` | 登录校验 |
| bcrypt | **4.0.1** | 4.0.1 | 密码哈希算法 | 同上 | hash 验证 |
| python-multipart | **0.0.20** | 0.0.20 | 文件上传表单 | documents upload | multipart |
| langgraph | **1.1.10** | 1.1.10 | Workflow 能力（KPI/Task 辅线相关栈） | workflow 相关 | 非 LLM Gateway 主链 |
| langchain-core | （langgraph 依赖） | **1.4.8** | LangChain 核心依赖 | 间接依赖 | 不直连业务 SDK 调用 LLM |

### B.3 总表：Frontend 依赖（package.json 范围 + lock 解析）

| 包名 | package.json | package-lock 解析 | 用途 | 关联源码 |
|---|---|---|---|---|
| react | **^19.1.0** | **19.2.7** | UI | `frontend/src/*` |
| react-dom | **^19.1.0** | **19.2.7** | DOM 渲染 | `main.tsx` |
| vite | **^7.0.0** | **7.3.6** | dev server / build | `vite.config.ts` |
| typescript | **^5.8.3** | **5.9.3** | 类型检查 | `tsconfig*.json` |
| vitest | **^3.2.4** | **3.2.6** | 单测 | `*.test.ts(x)` |
| @vitejs/plugin-react | **^4.6.0** | **4.7.0** | React 插件 | Vite 配置 |
| @testing-library/react | **^16.3.0** | **16.3.2** | 组件测试 | 页面测试 |
| @testing-library/jest-dom | **^6.6.3** | **6.9.1** | DOM matcher | test setup |
| jsdom | **^26.1.0** | **26.1.0** | 测试 DOM | vitest 环境 |

### B.4 总表：平台能力组件（非 npm/pip 包，但是面试知识点）

| 能力 | 当前实现版本/标识 | 源码/表 | 知识点必须会说的内容 |
|---|---|---|---|
| 正式 Repository | PostgreSQL（不是 InMemory） | `repositories/postgres/*` | 页面与企业验收权威 |
| InMemory | 测试适配器 | inmemory 实现 | 仅 unittest / 故障隔离 |
| Alembic head | **`20260717_08_ai_runtime`** | `alembic/versions/20260717_08_ai_runtime_settings.py` | Runtime 持久化迁移 |
| JWT | PyJWT 2.10.1 + 自研 service | `security/jwt_service.py` | Access Token，不塞权限列表 |
| RBAC | 冻结 Permission Registry | `security/permission_registry.py` | admin/manager/employee |
| LLM 默认模式 | **stub** | Settings / Runtime | 默认零真实费用 |
| AI Runtime 表 | `ai_runtime_settings` | migration 08 | mode/kill_switch/version，无 Secret |
| LLM Gateway | 自研 | `llm/gateway.py` | 唯一 Provider 外呼 |
| Fallback 顺序 | OpenRouter→NVIDIA→Gemini→Local Qwen | `llm/provider_chain.py` | 固定串行 + 熔断 |
| 金额 | Decimal | Ledger 相关 | 禁止 float |
| 测试基线 | PG **297/6 skip**；IM **286/62 skip**；FE **116/116** | tests | 可核对数字 |
| 本地入口 | **5173** | `scripts/start_local.sh` | 完整启动后可业务测 |
| Compose 入口 | **8080** | `scripts/compose_up.sh` | Nginx FE |

### B.5 逐项知识点卡（填写版，不是空表）

#### 1) FastAPI 0.136.3

| 字段 | 内容 |
|---|---|
| 版本 | **0.136.3**（`backend/requirements.txt`；venv 一致） |
| 解决什么业务问题 | 登录、文档、RAG、AI、报告、审批、Runtime 的统一 HTTP 入口 |
| 在 ERIP 中的位置 | Route 层；薄控制器 |
| 输入 | HTTP + JWT + schema |
| 输出 | `ApiResponse` / SSE(Task) / 错误码 |
| 不负责 | 直接调 LLM SDK、直接拼 SQL 业务规则 |
| 关联技术 | Pydantic 2.13.4、Uvicorn 0.49.0、Starlette 1.3.1 |
| 源码 | `backend/app/api/*.py`, `main.py` |
| TL 追问 | 为什么不把 Gateway 逻辑写在 Route？→ 边界清晰、可测、可替换 |

#### 2) React 19（lock 19.2.7）

| 字段 | 内容 |
|---|---|
| 版本 | 声明 `^19.1.0`，lock **19.2.7** |
| 解决什么 | 正式业务页面与权限 UX |
| 页面 | 学习总览 / 文書管理 / RAG·AI分析 / KPI任务分析 / 承認管理 / AI管理 |
| 关键组件 | AuthContext、ProtectedRoute、各 Page |
| 安全 | Token 仅 `sessionStorage`（`erip.access_token`） |
| 不负责 | 服务端授权最终判定 |
| 源码 | `frontend/src/App.tsx`, `pages/*`, `auth/*` |

#### 3) Vite 7（lock 7.3.6） + TypeScript 5.9.3

| 字段 | 内容 |
|---|---|
| 版本 | Vite 声明 `^7.0.0` → lock **7.3.6**；TS 声明 `^5.8.3` → lock **5.9.3** |
| 用途 | 本地 dev（5173）、production build |
| Compose | 构建产物由 Nginx 托管，不是浏览器直连 Vite |
| 测试 | Vitest **3.2.6**，FE **116/116** |

#### 4) PostgreSQL 16 + pgvector 镜像 + Alembic 1.16.5

| 字段 | 内容 |
|---|---|
| PG | 镜像 **pgvector/pgvector:pg16** |
| 驱动 | psycopg **3.2.9** |
| ORM | SQLAlchemy **2.0.51** |
| Migration | Alembic **1.16.5**，head **`20260717_08_ai_runtime`** |
| 正式性 | 页面、Approval、Audit、Ledger、Runtime 权威 |
| InMemory | 仅自动化测试适配器 |

#### 5) JWT / RBAC（PyJWT 2.10.1）

| 字段 | 内容 |
|---|---|
| 版本 | PyJWT **2.10.1**；passlib **1.7.4**；bcrypt **4.0.1** |
| 流程 | login → Access Token → `/users/me` → permission 镜像 |
| 角色 | admin / manager / employee |
| fail-closed | 未知 role 空权限 |
| 401/403 | 清会话 / 保会话 |
| 源码 | `backend/app/security/*`, `frontend/src/auth/*` |

#### 6) 文档主链 + RAG

| 字段 | 内容 |
|---|---|
| 版本依赖 | FastAPI + multipart 0.0.20 + PG |
| 步骤 | Upload → Import → Chunk → Retrieval → Internal RAG |
| Citation | 必须可追溯；证据不足可拒绝 |
| 默认 LLM | 普通 RAG **不**走真实 Provider |
| 页面衔接 | `/documents` → `/rag?document_id=` |

#### 7) LLM Gateway / 双路由 / Ledger

| 字段 | 内容 |
|---|---|
| 版本依赖 | httpx **0.28.1**；金额 Decimal；默认 stub |
| 入口 | 仅 `LLMGatewayService` |
| 路由 | `ai_analysis=low_cost`；`executive_report=high_quality` |
| 控制 | Evidence Gate、Idempotency-Key、quota、Ledger |
| Fallback | OpenRouter→NVIDIA→Gemini→Local Qwen |
| Runtime | 表 `ai_runtime_settings`；Secret 不入库 |
| 诚实边界 | 未跑真实付费 smoke ≠ 已验证真实模型效果 |

#### 8) Approval + ReportVersion + Audit

| 字段 | 内容 |
|---|---|
| 版本依赖 | PG 事务/行锁 + Alembic 04+ |
| 状态机 | pending_approval / approved / rejected / revised… |
| HTTP | submit **201**；employee approve **403**；冲突 **409** |
| Version | 不可变快照，改订出新版 |
| Audit | append-only；request_id；无 Key/全文 |

#### 9) Docker / Compose

| 字段 | 内容 |
|---|---|
| Backend 镜像基座 | `python:3.12-slim` |
| Frontend build | `node:22-alpine` |
| Frontend runtime | `nginx:1.29-alpine` |
| DB 镜像 | `pgvector/pgvector:pg16` |
| 端口 | 本地完整开发 **5173**；Compose FE **8080** |
| 启动 | `start_local.sh` / `compose_up.sh` |
| 注意 | 5173 与 8080 数据源不同；完整启动才可业务测 |

### B.6 30 秒技术版本口述模板

```text
Backend は Python 3.12、FastAPI 0.136.3、Uvicorn 0.49.0、Pydantic 2.13.4、
Alembic 1.16.5、psycopg 3.2.9、PyJWT 2.10.1。
Frontend は React 19（lock 19.2.7）、Vite 7.3.6、TypeScript 5.9.3、Vitest 3.2.6。
DB は PostgreSQL 16 + pgvector イメージ、Compose Frontend は Nginx 1.29。
正式 Repository は PostgreSQL、既定 LLM は stub、Alembic head は 20260717_08_ai_runtime。
```

---

## 第二章 FastAPI 与统一 HTTP 边界

### 客户为什么需要

经营分析请求由前端与脚本发起：登录、文档、检索、显式 AI、董事会报告、审批、AI Runtime 管理。FastAPI 负责统一 HTTP 边界、校验、依赖注入、错误码与 request_id。

### 技术版本（本章）

| 项 | 版本 | 来源 |
|---|---|---|
| FastAPI | **0.136.3** | `backend/requirements.txt` / venv |
| Starlette | **1.3.1** | venv（FastAPI 依赖） |
| Uvicorn | **0.49.0** | requirements |
| Pydantic | **2.13.4** | requirements |
| pydantic-settings | **2.14.1** | requirements |

### 当前主要路由族

| 路由族 | 前缀 / 路径 | 职责 |
|---|---|---|
| Auth | `POST /api/v1/auth/login` | 签发 JWT Access Token |
| Security | `GET /api/v1/users/me` 等 | 当前用户、角色/权限目录 |
| Documents | `/api/v1/documents` | 上传、列表、详情、归档 |
| Import / Chunk | documents 子路径 | Import 流水、Chunk 生成 |
| Retrieval | `/api/v1/document-retrieval` | 文档检索 |
| Internal RAG | `/api/v1/internal-rag` | 带 Citation 的 grounded answer |
| AI Analysis | `POST /api/v1/ai-analysis` | 显式 low_cost 分析 |
| Executive Report | `POST /api/v1/executive-reports` | high_quality 董事会报告 |
| Reports Catalog | `GET /api/v1/reports` | 审批页报告目录 |
| Approvals | `/api/v1/reports/{task_id}/submit-approval` 等 | 审批状态机 |
| AI Runtime Admin | `GET/PATCH /api/v1/admin/ai-runtime` | 持久化 mode / kill_switch |
| LLM Admin（兼容） | `/api/v1/admin/llm/runtime` | 兼容保留 |
| Tasks | `/api/tasks` | KPI 任务分析（辅线） |
| Health | `GET /health` | 健康与 repository_backend |

### 设计要点

- Route 只做 HTTP / 鉴权依赖 / schema；业务在 Service。
- 业务层禁止直接调用 LLM SDK；收费 side-effect 只经 `LLMGatewayService`。
- 统一 `ApiResponse` 与错误结构；日志含 `request_id`、`task_id`（如有）、`event`、`status`。

【源码映射】

```text
backend/app/main.py
backend/app/api/*.py
backend/app/schemas/
backend/app/services/
```

【TL Review】

- schema 是否清晰、版本是否稳定。
- 401/403 是否 fail-closed。
- 是否把 LLM 调用藏进普通 RAG 路径。
- 能否报出 FastAPI **0.136.3** 与来源文件。

---

## 第三章 JWT / RBAC

### 技术版本（本章）

| 项 | 版本 | 来源 |
|---|---|---|
| PyJWT | **2.10.1** | `backend/requirements.txt` |
| passlib | **1.7.4** | requirements |
| bcrypt | **4.0.1** | requirements |
| Frontend Token 存储 | sessionStorage 键 `erip.access_token` | `AuthContext.tsx` |

### 角色与权限（冻结）

角色：`admin` / `manager` / `employee`

权限目录（后端 `PermissionRegistry` 与前端 `permissions.ts` 镜像）：

```text
documents.read / documents.write / documents.archive
retrieval.query
analysis.execute
approval.submit / approval.review / approval.admin
audit.read
security.manage
```

| 角色 | 关键能力 |
|---|---|
| admin | 全部权限（含 `security.manage` → AI管理页） |
| manager | 文档/检索/分析/审批（含 review·admin）/审计 |
| employee | 文档读写、检索、分析、提交审批（不可 approve） |

### 关键规则

- JWT 只承载身份；权限由服务端 Registry 按 role 推导。
- 未知角色 → 空权限（fail-closed）。
- Frontend：`ProtectedRoute` 拦截未登录；导航与按钮按 permission 隐藏/禁用。
- Backend：`require_permission()` 才是权威；前端 RBAC 只做 UX。
- Token 存放：`sessionStorage` 键 `erip.access_token`；不写 localStorage。
- 401：清会话；403：保会话，展示拒绝。

【源码映射】

```text
backend/app/security/
frontend/src/auth/AuthContext.tsx
frontend/src/auth/permissions.ts
frontend/src/routing/ProtectedRoute.tsx
```

---

## 第四章 文档生命周期

### 技术版本（本章）

| 项 | 版本/标识 | 来源 |
|---|---|---|
| python-multipart | **0.0.20** | requirements（上传表单） |
| PostgreSQL / 驱动 | PG16 镜像 + psycopg **3.2.9** | compose + requirements |
| 持久化 | documents / upload_* / document_imports / document_chunks | DATABASE / migrations |

### 业务顺序

```text
Upload（upload_sessions + 幂等键）
→ documents（含 content）
→ Import（document_imports）
→ Chunk（document_chunks，可选 embedding）
→ Retrieval / Internal RAG
→ Archive（可选）
```

### 为什么需要分层

- Upload 负责文件与会话校验。
- Import 负责业务入库状态。
- Chunk 负责检索单元；可 replace。
- Retrieval 只读，不写分析费用。

### 页面衔接

- 文書管理页列表展示 title / id / status / created / archived / chunk_count / searchable。
- 「使用此文档检索」→ `/rag?document_id=`。
- Scenario01 种子：`scripts/seed_scenario01.sh`（PostgreSQL-only、幂等、零 Provider 调用）。

【源码映射】

```text
backend/app/services/document_upload_service.py
backend/app/services/document_import_service.py
backend/app/services/document_chunk_service.py
backend/app/services/document_retrieval_service.py
frontend/src/pages/DocumentsPage.tsx
```

---

## 第五章 RAG 与 Citation

### 普通 RAG（Internal RAG）

- 在 Document Retrieval 之上组装 grounded answer。
- 默认路径**不调用真实付费 LLM**；deterministic / extractive 合成 + Citation。
- 可要求 citations；证据不足则业务拒绝，而不是胡编。

### 与显式 AI 分析的边界

| 路径 | 是否默认调 Gateway | 费用 | 用途 |
|---|---|---|---|
| Retrieval / Internal RAG | 否 | 零（默认） | 检索与引用回答 |
| AI 分析 `POST /api/v1/ai-analysis` | 是（low_cost） | 受 Ledger 约束 | 显式确认后的分析 |
| 董事会报告 | 是（high_quality） | 受 Ledger 约束 | 経営会议报告 |

【为什么这样设计】

经营会议要根拠。普通检索应低成本、可审计；只有用户显式确认才进入可能收费的 AI 路径。

【源码映射】

```text
backend/app/services/internal_rag_service.py
backend/app/services/rag_answer_generator.py
backend/app/services/reranker_service.py
frontend/src/pages/RagPage.tsx
```

---

## 第六章 LLM Gateway 与双路由

### 技术版本（本章）

| 项 | 版本/标识 | 来源 |
|---|---|---|
| httpx | **0.28.1** | requirements（Provider 出站） |
| 默认 mode | **stub** | Settings / `ai_runtime_settings` / compose |
| Runtime 表 | `ai_runtime_settings` | Alembic **`20260717_08_ai_runtime`** |
| 金额类型 | Decimal（禁止 float） | Ledger / Operation Policy |
| Fallback | OpenRouter→NVIDIA→Gemini→Local Qwen | `provider_chain.py` |

### Gateway 唯一入口

`LLMGatewayService` 是唯一允许调用 Provider 的组件。业务 Router / Service 不得直连 OpenAI SDK 或第三方 SDK。

### Operation Policy

| Operation | route_tier | 要点 |
|---|---|---|
| `ai_analysis` | `low_cost` | Evidence Gate、confirmed、Idempotency-Key、额度预占 |
| `executive_report` | `high_quality` | 需成功分析等策略；生成 ReportVersion；**不自动审批** |

金额用 **Decimal**，禁止 float。

### Provider 与默认模式

- 默认：`LLM_PROVIDER_MODE=stub` / `effective_mode=stub`
- 可选：`openrouter` / `fallback_chain`
- Fallback 固定串行：OpenRouter → NVIDIA → Gemini → Local Qwen
- 熔断状态在 PostgreSQL（`llm_provider_circuit_state`）
- 尝试明细：`llm_provider_attempts`
- 账本：`llm_usage_ledger` + `llm_quota_buckets`
- 分析结果：`ai_analysis_results`

### AI Runtime 持久化

表：`ai_runtime_settings`（单例）

- 字段：mode / kill_switch / version / actor 等
- **Secret 永不入库、永不进 API 响应**
- API：`GET/PATCH /api/v1/admin/ai-runtime`（`security.manage`）
- InMemory 后端对该管理能力 **503 fail-closed**
- Kill Switch 强制 stub；乐观锁 `expected_version` 冲突 → 409
- 启用真实 LLM 需二次确认文案

【源码映射】

```text
backend/app/llm/gateway.py
backend/app/llm/operation_policy.py
backend/app/llm/provider_chain.py
backend/app/services/ai_analysis_service.py
backend/app/services/executive_report_service.py
backend/app/services/ai_runtime_service.py
backend/app/api/ai_runtime_admin.py
frontend/src/pages/AdminLlmPage.tsx
```

【TL Review】

- 是否把默认 stub 说成“已验证真实模型效果”。
- 是否承认默认测试不执行真实付费 LLM。

---

## 第七章 董事会报告与 Approval

### Report / ReportVersion

- `reports`：当前有效报告实体（与 task 关联）
- `report_versions`：不可变快照；修订产生新版本，不覆盖旧正文
- 报告生成成功返回 task_id / report_id / report_version_id
- 前端可「提交审批」→ `/approval?approval_id=`

### 审批状态机（企业能力）

```text
generated
→ submit → pending_approval
→ approve → approved
→ reject → rejected
→ revise → revised（新 ReportVersion）
→ resubmit → pending_approval
```

### 权限边界

| 动作 | 权限 | 典型结果 |
|---|---|---|
| submit | `approval.submit` | 201 `pending_approval` |
| list 队列 | `approval.review` | manager/admin |
| approve / reject | `approval.admin` | employee → **403** |
| revise / resubmit | submit + owner 或 admin | ownership 检查 |
| detail | review 或 owner | employee 可读自己的 |

并发：PostgreSQL 行锁 + partial unique（每 task 至多一个 pending）。

History：`approval_events` append-only；Audit：`audit_logs` 独立安全事实。

【源码映射】

```text
backend/app/api/approvals.py
backend/app/services/approval_service.py
backend/app/repositories/postgres/approval_repository.py
frontend/src/pages/ApprovalPage.tsx
```

---

## 第八章 Persistent Audit

```text
JWT CurrentUser / Permission
→ Persistent Audit Dependency
→ 业务 savepoint
  ├── success → append success audit → commit
  └── failure → rollback savepoint → append failure audit → commit audit → rethrow
```

禁止写入：API Key、Secret、完整 Prompt、文档全文、密码、Authorization Header。

贯穿字段：`request_id`、actor、action、resource、status、duration 等。

【源码映射】

```text
backend/app/services/persistent_audit_service.py
backend/app/api/persistent_audit.py
backend/app/repositories/postgres/audit_repository.py
```

---

## 第九章 PostgreSQL / pgvector / Alembic

### 技术版本（本章）

| 项 | 版本 | 来源 |
|---|---|---|
| PostgreSQL | **16**（镜像 `pgvector/pgvector:pg16`） | `docker-compose.yml` |
| psycopg | **3.2.9** | requirements |
| SQLAlchemy | **2.0.51** | venv / 运行栈 |
| Alembic | **1.16.5** | requirements |
| head revision | **`20260717_08_ai_runtime`** | `alembic/versions/` |

### 定位

| 实现 | 定位 |
|---|---|
| **PostgreSQL** | 正式 Repository；页面、企业验收、Stub E2E、审计、审批、Ledger 权威 |
| **InMemory** | 仅自动化单元测试适配器 / 故障隔离；**不是**正式页面或生产 Repository |

### 当前基线

- Alembic head：`20260717_08_ai_runtime`
- 业务表约 18 + `alembic_version`
- 含 documents、chunks、reports、approvals、audit_logs、llm_*、`ai_runtime_settings` 等
- Compose：`REPOSITORY_BACKEND=postgres` 写死
- 本地完整开发：宿主 PostgreSQL 库 `erip_local` + Backend + Vite

【源码映射】

```text
backend/alembic/versions/
backend/app/repositories/postgres/
docs/database/DATABASE.md
```

---

## 第十章 Frontend 正式页面

### 技术版本（本章）

| 项 | package.json | package-lock 解析 |
|---|---|---|
| react / react-dom | ^19.1.0 | **19.2.7** |
| vite | ^7.0.0 | **7.3.6** |
| typescript | ^5.8.3 | **5.9.3** |
| vitest | ^3.2.4 | **3.2.6** |
| @vitejs/plugin-react | ^4.6.0 | **4.7.0** |
| 测试结果 | — | Frontend **116/116**；production build 通过 |

| 导航标签 | 路径 | 权限要点 |
|---|---|---|
| 学习总览 | `/dashboard` | 已登录 |
| 文書管理 | `/documents` | `documents.read` |
| RAG/AI分析 | `/rag` | `retrieval.query` 或 `analysis.execute` |
| KPI任务分析 | `/analysis` | `analysis.execute` |
| 承認管理 | `/approval` | submit / review / admin 任一 |
| AI管理 | `/ai-admin` | `security.manage` |

附加能力：

- AuthContext + ProtectedRoute
- Learning Trace / Lifecycle Live Status（本地 ring buffer，**不回传 Backend**，不存 Token 原文）
- Document→RAG query handoff；Report→Approval handoff

【源码映射】

```text
frontend/src/App.tsx
frontend/src/pages/*
frontend/src/learning/*
```

---

## 第十一章 部署与运行模式

### 技术版本（本章）

| 组件 | 镜像/版本 | 来源 |
|---|---|---|
| Backend base | `python:3.12-slim` | `backend/Dockerfile` |
| Frontend build | `node:22-alpine` | `frontend/Dockerfile` |
| Frontend runtime | `nginx:1.29-alpine` | `frontend/Dockerfile` |
| Database | `pgvector/pgvector:pg16` | `docker-compose.yml` |
| Compose project | `erip`；services `postgres`/`backend`/`frontend` | compose 文件 |

| 方式 | 组成 | 页面端口 | Docker |
|---|---|---|---|
| 本地完整开发 | 宿主 PostgreSQL + FastAPI + Vite | **5173** | 否 |
| Docker Compose | postgres + backend + nginx frontend | **8080** | 是 |
| 正式生产 | HTTPS + 内网 Backend + 独立 PG | 域名 | 视平台 |

要点：

- `./scripts/start_local.sh`：本地完整开发；5173 **在完整启动后**可做真实业务测试。
- **单独**只起 Vite、没有 Backend/PostgreSQL，则无法完成业务测试。
- `./scripts/compose_up.sh`：验收/演示；Volume `erip_postgres_data`；禁止随意 `down -v`。
- 5173 与 8080 **不是同一套数据源**。
- 默认零真实 LLM；`RUN_REAL_LLM_SMOKE=0`。

【源码映射】

```text
docker-compose.yml
scripts/start_local.sh
scripts/compose_up.sh
scripts/seed_scenario01.sh
docs/development/DEPLOYMENT_GUIDE.md
```

---

## 第十二章 验收数字与诚实边界

### V1.0 验收基线（仓库事实）

| 项 | 值 |
|---|---|
| 正式 Repository | PostgreSQL |
| InMemory | 仅 unittest / 故障隔离 |
| Alembic head | `20260717_08_ai_runtime` |
| PostgreSQL Backend | **297 tests / 6 skipped** |
| InMemory Backend | **286 tests / 62 skipped** |
| Frontend | **116/116**；production build 通过 |
| 默认 LLM | stub |
| Runtime 表 | `ai_runtime_settings` |

### 已交付（可讲）

JWT/RBAC、文档主链、RAG+Citation、显式 AI、董事会报告、Approval API、Persistent Audit、LLM Ledger、Fallback、Compose+Alembic、正式 Frontend、AI Runtime 持久化、页面衔接、Scenario01 seed。

### 未完成（禁止夸大）

- 未执行真实付费 smoke → **不能**声称已验证真实模型效果
- Billing UI / 多租户预算管理 UI
- SIEM / WORM / 全量 Streaming 生产方案
- DeepSeek 默认启用
- Redis / RabbitMQ / K8s 作为本仓默认可运行栈

---

<!-- DOC-SYNC:START group=architecture -->
## 文档同步块

- group: `architecture`
- file: `ai-agent-retail-handbook-v3/03_AI核心知识.md`
- self_sha256: `pending-sync`
- peers: 由 `scripts/sync_retail_handbook_docs.py` 维护

说明：
- 这个块由 `scripts/sync_retail_handbook_docs.py` 自动维护。
- 只同步这个块，不覆盖各自正文。
<!-- DOC-SYNC:END group=architecture -->
