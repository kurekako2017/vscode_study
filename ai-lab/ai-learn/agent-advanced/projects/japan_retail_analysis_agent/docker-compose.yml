services:
  backend:
    build: .
    ports:
      - "8020:8020"
    environment:
      APP_ENV: docker
      APP_HOST: 0.0.0.0
      APP_PORT: 8020
      CORS_ALLOWED_ORIGINS: http://127.0.0.1:5178,http://localhost:5178
      RUNTIME_DIR: /app/runtime
    volumes:
      - ./runtime:/app/runtime

