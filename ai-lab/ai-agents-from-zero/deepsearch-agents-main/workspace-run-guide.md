# DeepSearch Agents 工作区启动指南

主运行手册已迁移到 [DeepSearch Agents 工作区启动指南.md](./DeepSearch%20Agents%20工作区启动指南.md)。

如果你只想快速启动当前推荐环境，直接看这份主指南即可：

- `OpenRouter` 作为模型入口
- `NAS MySQL` 作为结构化数据源
- `本地知识库` 默认从 `docs/knowledge_base/` 读取，不需要单独部署 RAGFlow
