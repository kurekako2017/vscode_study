# softbs/support 目录说明

本目录用于存放软性支撑资料，不作为主学习线入口。

## 目录结构

- `history/`：历史环境说明、一次性配置记录、迁移过程留档
- `notes/`：临时笔记、零散记录、短文本草稿
- `tools/`：教程配套工具示例脚本和请求文件

## 建议

- 需要系统阅读的主教程继续放在 `github/`、`vscode/`、`aider/`、`openclaw/` 等主题目录
- 只用于留痕或临时记录的内容统一放到 `support/`
- 需要长期复用的通用脚本仍然放在 `scripts/`