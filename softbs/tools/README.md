# softbs/tools 目录说明

本目录用于存放 softbs 学习资料配套的工具示例文件，不作为仓库级通用脚本入口。

## 当前文件

- `ai-run.ps1`：多模型 API 调用演示脚本（教程示例）
- `openai.http`：REST Client 请求示例

## 约定

- 需要长期复用、可直接执行的通用脚本统一放在 `scripts/`
- `softbs/tools` 只保留教程/示例性质的辅助文件
